begin
	int a, b, c, d
	a := 1
	b := 2

	if a < b
		puts "Entrei no if a menor que b"
	else
		puts "Entrei no else a menor que b"
	endif

	if b < a
		puts "Entrei no if b menor que a"
	else
		puts "Entrei no else b menor que a"
	endif

	c := 4
	d := 5
	while c < d
		puts c
		c := 6
	endwhile
	puts c
end